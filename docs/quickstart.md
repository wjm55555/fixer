# Quickstart

```bash
bun install
```

```bash
bun run examples/repair-demo.ts /path/to/rust-project /path/to/output /path/to/c-source
```

The output directory will contain a `.fixer/` folder with logs and patch history.
